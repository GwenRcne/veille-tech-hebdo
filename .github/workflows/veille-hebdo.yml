name: Veille Hebdomadaire

on:
  schedule:
    # Tous les lundis à 9h00 (heure UTC)
    - cron: '0 9 * * 1'
  
  # Permet l'exécution manuelle
  workflow_dispatch:

jobs:
  veille:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
    
    steps:
    - name: 📥 Récupération du code
      uses: actions/checkout@v3
    
    - name: 🐍 Installation de Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: 📦 Installation des dépendances
      run: |
        pip install feedparser requests
    
    - name: 🚀 Exécution de la veille
      run: |
        python veille.py
    
    - name: 📤 Publication des changements
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add docs/
        git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Mise à jour hebdomadaire automatique" && git push)
